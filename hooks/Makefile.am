hooksdir=@LXCHOOKDIR@

hooks_SCRIPTS = \
	clonehostname \
	mountecryptfsroot \
	ubuntu-cloud-prep \
	squid-deb-proxy-client

hooks_PROGRAMS = \
	unmount-namespace

unmount_namespace_SOURCES = \
	unmount-namespace.c

EXTRA_DIST=$(hooks_SCRIPTS)
